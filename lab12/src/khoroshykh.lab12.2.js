"use strict";
const BLUE = '#0000ff';
const GREEN = '#008000';
const YELLOW = '#ffff00';
const colors = [BLUE, GREEN, YELLOW];
const fields = document.querySelectorAll(".counters_item");

if (fields.length) { 

   for (let elem of fields) { 

      console.log(elem);
   }
}
