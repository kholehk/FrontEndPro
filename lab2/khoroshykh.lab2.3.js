const aValue = prompt("Value a:");
const bValue = prompt("Value b:");
const cValue = prompt("Value c:");
const result = (+aValue + +bValue + +cValue) / 3;

console.log(`a:${aValue}`, `b:${bValue}`, `c:${cValue}`, `result:${result}`);
