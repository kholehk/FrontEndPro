const a = 15;
const b = 35;

if (a > b) {
  console.log(`a:${a} greater than b:${b}`);
} else if (a < b) {
  console.log(`a:${a} smaller than b:${b}`);
} else {
  console.log(`a:${a} equals b:${b}`);
}
